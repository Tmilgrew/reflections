When you initialize a repository, you create a form for logging to take place so we can use the version control

the staging area is where we put files ready to be committed.  The working directory is below that.  